import Link from 'next/link';
import { AlertTriangle, RefreshCw } from 'lucide-react';
export function DataError({ title = 'Chưa tải được dữ liệu', message = 'Kiểm tra kết nối rồi thử lại. Đây không phải danh sách trống.', onRetry, busy = false }: { title?: string; message?: string; onRetry: () => void; busy?: boolean }) {
  return <div role="alert" className="rounded-2xl border border-amber-500/30 bg-amber-500/10 p-5 space-y-3"><div className="flex items-start gap-3"><AlertTriangle size={20} className="mt-0.5 shrink-0 text-amber-300" /><div><h2 className="font-semibold text-amber-100">{title}</h2><p className="mt-2 text-sm leading-6 text-amber-200/80">{message}</p></div></div><div className="flex flex-wrap gap-3"><button type="button" disabled={busy} onClick={onRetry} className="inline-flex min-h-11 items-center gap-2 rounded-xl border border-amber-300/30 px-4 py-2 text-sm font-semibold text-amber-100 disabled:opacity-50"><RefreshCw size={16} className={busy ? 'animate-spin' : ''} />{busy ? 'Đang tải…' : 'Thử lại'}</button><Link href="/#setup" className="inline-flex min-h-11 items-center px-2 text-sm text-amber-100 underline">Kiểm tra kết nối</Link></div></div>;
}
